# Changelog

## [Unreleased]

### Added

- `--install-system-deps` como opt-in explícito para instalar `wmctrl`/`xdotool` faltantes sin ejecutar `sudo` por defecto
- extracción inicial del notifier desde `super-turing-opencode` a un addon separado
- scripts `install.sh`, `status.sh` y `uninstall.sh`
- `notify.example.json` como referencia de configuración local
- `INSTALLATION.md` con prerequisitos, validación y notas Linux/GNOME/X11
